import 'package:flutter/material.dart';

class PlayerThumbnailWidget extends StatelessWidget {
  const PlayerThumbnailWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
